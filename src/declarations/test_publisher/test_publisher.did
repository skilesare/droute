service : {
  test: () -> (variant {
                 fail: text;
                 success;
               });
  testSimpleNotify: () -> (variant {
                             fail: text;
                             success;
                           });
}
